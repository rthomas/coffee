[package]
name = "coffee-rpc-server"
version = "0.1.0"
authors = ["ryan"]
edition = "2018"

[dependencies]
coffee-common = {path = "../coffee-common"}

tonic = "0.2.0"
tokio = { version = "0.2", features = ["macros"] }